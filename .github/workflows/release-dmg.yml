name: Build and Release DMG

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: # Allow manual triggering
    inputs:
      create_release:
        description: "Create GitHub Release"
        required: false
        default: "false"
        type: boolean

jobs:
  build-dmg:
    name: Build DMG for Release
    # Only run when:
    # 1. A tag starting with 'v' is pushed (changesets creates tags automatically)
    # 2. Manually triggered
    if: |
      (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')) ||
      github.event_name == 'workflow_dispatch'

    runs-on: macos-latest
    environment: "DMG Build Environment"

    permissions:
      contents: write # Required to create releases

    steps:
      - name: Verify signing secrets are available
        run: |
          MISSING=""
          [ -z "$CSC_LINK" ] && MISSING="$MISSING CSC_LINK"
          [ -z "$CSC_KEY_PASSWORD" ] && MISSING="$MISSING CSC_KEY_PASSWORD"
          # Notarization is currently disabled; uncomment these checks when re-enabled:
          # [ -z "$APPLE_ID" ] && MISSING="$MISSING APPLE_ID"
          # [ -z "$APPLE_APP_SPECIFIC_PASSWORD" ] && MISSING="$MISSING APPLE_APP_SPECIFIC_PASSWORD"
          # [ -z "$APPLE_TEAM_ID" ] && MISSING="$MISSING APPLE_TEAM_ID"
          if [ -n "$MISSING" ]; then
            echo "::error::Missing required environment secrets:$MISSING"
            echo "Ensure the 'DMG Build Environment' environment is configured with all required secrets."
            exit 1
          fi
          echo "All signing secrets are set."
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"
          cache: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: src/frontend/package-lock.json

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache LFS objects
        uses: actions/cache@v4
        with:
          path: .git/lfs
          key: lfs-${{ runner.os }}-${{ hashFiles('.gitattributes') }}
          restore-keys: |
            lfs-${{ runner.os }}-

      - name: Cache Rust/Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            build/tokenizers/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache tokenizers library
        uses: actions/cache@v4
        with:
          path: build/tokenizers/libtokenizers.a
          key: ${{ runner.os }}-tokenizers-${{ hashFiles('build/tokenizers/**/*.rs', 'build/tokenizers/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-tokenizers-

      - name: Cache ONNX Runtime library
        uses: actions/cache@v4
        with:
          path: build/libonnxruntime.*.dylib
          key: ${{ runner.os }}-onnx-1.23.1
          restore-keys: |
            ${{ runner.os }}-onnx-

      - name: Extract Version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            # Extract version from tag (v1.2.3 -> 1.2.3)
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            # For non-tag builds, extract version from package.json
            VERSION=$(cat src/frontend/package.json | grep '"version"' | head -1 | awk -F '"' '{print $4}')
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Verify Git LFS Files
        run: |
          git lfs pull
          # Verify model file is not an LFS pointer
          MODEL_SIZE=$(stat -f%z "model/quantized/model_quantized.onnx" 2>/dev/null || echo "0")
          echo "Model file size: ${MODEL_SIZE} bytes"
          if [ "$MODEL_SIZE" -lt 1000 ]; then
            echo "❌ Model file appears to be an LFS pointer (too small)"
            exit 1
          fi

      - name: Install Python Dependencies
        run: |
          uv venv --python 3.13
          uv pip install onnxruntime

      - name: Install Root Dependencies
        run: npm ci

      - name: Install Frontend Dependencies
        run: |
          cd src/frontend
          npm ci

      - name: Build DMG
        run: make build-dmg
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

          # Update CSC_LINK:**
          # 1. In [Apple Developer Portal](https://developer.apple.com/account/resources/certificates/list), create or find a **"Developer ID Application"** certificate (under "Software" → "Developer ID Application")
          # 2. Export it as a `.p12` file from Keychain Access (with a password)
          # 3. Base64-encode it: `base64 -i certificate.p12 | pbcopy`
          # 4. Update the `CSC_LINK` secret in your GitHub environment with the base64 string
          # 5. Update `CSC_KEY_PASSWORD` with the `.p12` export password
          # The "Apple Development" certificate type is for local development/testing via Xcode. Only "Developer ID Application" certificates are accepted by Apple's notarization service for distributing outside the App Store.

      - name: Rename DMG with Version
        run: |
          cd src/frontend/release
          for dmg in *.dmg; do
            if [ -f "$dmg" ]; then
              # Replace spaces with hyphens for cleaner filename
              # Example: "Kiji Privacy Proxy-0.2.0.dmg" -> "Kiji-Privacy-Proxy-0.2.0.dmg"
              NEW_NAME=$(echo "$dmg" | tr ' ' '-')
              if [ "$dmg" != "$NEW_NAME" ]; then
                mv "$dmg" "$NEW_NAME"
                echo "Renamed: $dmg -> $NEW_NAME"
              else
                echo "Already clean: $dmg"
              fi
            fi
          done

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kiji-privacy-proxy-${{ steps.version.outputs.version }}-macos
          path: |
            src/frontend/release/*.dmg
            src/frontend/release/*.zip
            src/frontend/release/latest-mac.yml
          retention-days: 90

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v') || github.event.inputs.create_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            src/frontend/release/*.dmg
            src/frontend/release/*.zip
            src/frontend/release/latest-mac.yml
          draft: false
          prerelease: ${{ contains(github.ref, '-beta') || contains(github.ref, '-alpha') || contains(github.ref, '-rc') }}
          fail_on_unmatched_files: true
          make_latest: true
          append_body: false
          generate_release_notes: true
          body: |
            ## Kiji Privacy Proxy v${{ steps.version.outputs.version }}

            ### Downloads
            - **macOS**: DMG installer (Apple Silicon and Intel) - Desktop app with UI
            - **Linux**: Standalone binary archive (amd64) - API server only (no UI)

            ### macOS Installation

            **Quick Start:**
            1. Download `Kiji-Privacy-Proxy-${{ steps.version.outputs.version }}.dmg`
            2. Open the DMG and drag to Applications
            3. Launch "Kiji Privacy Proxy"

            **HTTPS Support - Install CA Certificate:**

            For HTTPS interception to work, you must trust the proxy's CA certificate:

            ```bash
            # System-wide trust (recommended)
            sudo security add-trusted-cert \
              -d \
              -r trustRoot \
              -k /Library/Keychains/System.keychain \
              ~/.kiji-proxy/certs/ca.crt
            ```

            Or use **Keychain Access**:
            1. Open Keychain Access app
            2. File → Import Items → `~/.kiji-proxy/certs/ca.crt`
            3. Double-click "Kiji Privacy Proxy CA" certificate
            4. Expand **Trust** → Set to **Always Trust**

            ### Linux Installation

            **Note:** Linux build is API-only (no desktop UI). Access via HTTP endpoints.

            **Quick Start:**
            ```bash
            # Extract
            tar -xzf kiji-privacy-proxy-${{ steps.version.outputs.version }}-linux-amd64.tar.gz
            cd kiji-privacy-proxy-${{ steps.version.outputs.version }}-linux-amd64

            # Run
            ./run.sh
            ```

            **HTTPS Support - Install CA Certificate:**
            ```bash
            # Ubuntu/Debian
            sudo cp ~/.kiji-proxy/certs/ca.crt /usr/local/share/ca-certificates/kiji-proxy-ca.crt
            sudo update-ca-certificates

            # RHEL/CentOS/Fedora
            sudo cp ~/.kiji-proxy/certs/ca.crt /etc/pki/ca-trust/source/anchors/kiji-proxy-ca.crt
            sudo update-ca-trust
            ```

            **Configuration:**
            ```bash
            # Set API key
            export OPENAI_API_KEY="your-key-here"

            # Check health
            curl http://localhost:8080/health

            # Test proxy
            curl -x http://localhost:8080 https://api.openai.com/v1/models
            ```

            See included `README.txt` for systemd service setup and advanced configuration.

            ### Documentation

            - [Getting Started Guide](https://github.com/dataiku/kiji-proxy/blob/main/docs/01-getting-started.md)
            - [Development Guide](https://github.com/dataiku/kiji-proxy/blob/main/docs/02-development-guide.md)
            - [Release Management](https://github.com/dataiku/kiji-proxy/blob/main/docs/04-release-management.md)

            ### What's New
            See the changelog below for details.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Release Summary
        if: always()
        run: |
          echo "## 📦 macOS Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**DMG Files:**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          ls -lh src/frontend/release/*.dmg 2>/dev/null | awk '{print "- " $9 " (" $5 ")"}' >> $GITHUB_STEP_SUMMARY || echo "No DMG files found" >> $GITHUB_STEP_SUMMARY

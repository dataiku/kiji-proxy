---
"kiji-privacy-proxy": minor
---

Rewrote the OpenAI provider to properly handle structured-output **schema** and response parsing, Persistent PII entity mapping across requests, Log Chrome-extension PII checks to `/logs`, Updated the app downloader
